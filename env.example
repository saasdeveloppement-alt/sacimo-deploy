# ============================================
# SACIMO - Variables d'environnement
# ============================================
# Copiez ce fichier vers .env.local et remplissez les valeurs
# cp env.example .env.local

# ============================================
# BASE DE DONNÉES (OBLIGATOIRE)
# ============================================
# URL de connexion PostgreSQL (format: postgresql://user:password@host:port/database)
# Pour Vercel, utilisez Vercel Postgres ou une base externe (Supabase, Neon, etc.)
DATABASE_URL="postgresql://user:password@localhost:5432/sacimo?schema=public"

# ============================================
# NEXT-AUTH (OBLIGATOIRE)
# ============================================
# Secret pour signer les tokens JWT (générez-en un avec: openssl rand -base64 32)
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# URL de votre application (en production: https://votre-domaine.vercel.app)
NEXTAUTH_URL="http://localhost:3000"

# ============================================
# API MELO.IO (OBLIGATOIRE)
# ============================================
# Clé API Melo.io (sandbox ou production)
MELO_API_KEY="your-melo-api-key-here"

# Environnement Melo.io: 'preprod' (sandbox) ou 'production'
MELO_ENV="preprod"

# ============================================
# GOOGLE OAUTH (OPTIONNEL)
# ============================================
# Pour activer la connexion Google, créez un projet sur Google Cloud Console
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"


# ============================================
# EMAIL (OPTIONNEL)
# ============================================
# Configuration pour l'envoi d'emails (SendGrid, SMTP, etc.)
EMAIL_SERVER_HOST="smtp.sendgrid.net"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="apikey"
EMAIL_SERVER_PASSWORD="your-sendgrid-api-key"
EMAIL_FROM="noreply@votre-domaine.com"

# ============================================
# AUTRES (OPTIONNEL)
# ============================================
# Clé secrète pour les endpoints de scraping (optionnel)
SCRAPE_SECRET_KEY="your-scrape-secret-key"

# ZenRows API (non utilisé en production, uniquement pour fallback)
# ZENROWS_API_KEY="your-zenrows-api-key"
# ZENROWS_KEY="your-zenrows-key"


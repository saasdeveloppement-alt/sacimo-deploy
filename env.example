# ============================================
# SACIMO - Variables d'environnement
# ============================================
# Copiez ce fichier vers .env.local et remplissez les valeurs
# cp env.example .env.local

# ============================================
# BASE DE DONNÉES (OBLIGATOIRE)
# ============================================
# URL de connexion PostgreSQL (format: postgresql://user:password@host:port/database)
# Pour Vercel, utilisez Vercel Postgres ou une base externe (Supabase, Neon, etc.)
DATABASE_URL="postgresql://user:password@localhost:5432/sacimo?schema=public"

# ============================================
# NEXT-AUTH (OBLIGATOIRE)
# ============================================
# Secret pour signer les tokens JWT (générez-en un avec: openssl rand -base64 32)
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# URL de votre application (en production: https://votre-domaine.vercel.app)
NEXTAUTH_URL="http://localhost:3000"

# ============================================
# API MELO.IO (OBLIGATOIRE)
# ============================================
# Clé API Melo.io (sandbox ou production)
MELO_API_KEY="your-melo-api-key-here"

# Environnement Melo.io: 'preprod' (sandbox) ou 'production'
MELO_ENV="preprod"

# ============================================
# GOOGLE OAUTH (OPTIONNEL)
# ============================================
# Pour activer la connexion Google, créez un projet sur Google Cloud Console
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ============================================
# GOOGLE CLOUD VISION & MAPS (OBLIGATOIRE pour localisation par images)
# ============================================
# ⚠️ IMPORTANT : Ces APIs nécessitent la facturation activée sur Google Cloud
# 1. Créez un projet sur https://console.cloud.google.com
# 2. Activez la facturation (crédit gratuit de $300 disponible)
# 3. Activez les APIs :
#    - Cloud Vision API
#    - Maps Geocoding API
#    - Maps Static API (pour Street View)
# 4. Créez une clé API dans APIs & Services > Credentials
# 5. Ajoutez les restrictions nécessaires (HTTP referrers, IPs, etc.)
# 
# Guide complet : https://console.cloud.google.com/apis/credentials
GOOGLE_CLOUD_VISION_API_KEY="your-google-cloud-vision-api-key"
GOOGLE_MAPS_API_KEY="your-google-maps-api-key"

# ============================================
# OPENAI API (OBLIGATOIRE pour localisation avancée)
# ============================================
# ⚠️ IMPORTANT : Nécessaire pour la détection de screenshots Google Maps et le raisonnement LLM
# 1. Créez un compte sur https://platform.openai.com
# 2. Générez une clé API dans Settings > API keys
# 3. Ajoutez des crédits à votre compte (minimum $5 recommandé)
# 4. La clé commence par "sk-"
# 
# Utilisations :
# - Détection robuste de screenshots Google Maps (GPT-4o-mini Vision)
# - Raisonnement LLM pour géolocalisation (dernier recours)
# - Génération d'explications structurées
# 
# Guide complet : https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-your-openai-api-key-here"


# ============================================
# EMAIL (OPTIONNEL)
# ============================================
# Configuration pour l'envoi d'emails (SendGrid, SMTP, etc.)
EMAIL_SERVER_HOST="smtp.sendgrid.net"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="apikey"
EMAIL_SERVER_PASSWORD="your-sendgrid-api-key"
EMAIL_FROM="noreply@votre-domaine.com"

# ============================================
# SUPABASE DVF (OPTIONNEL - pour estimation immobilière)
# ============================================
# URL de votre projet Supabase (ex: https://xxxxx.supabase.co)
# SUPABASE_URL="https://your-project.supabase.co"

# Service Role Key (clé secrète, à garder privée)
# Récupérable dans Settings > API > service_role key
# SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"

# ============================================
# AUTRES (OPTIONNEL)
# ============================================
# Clé secrète pour les endpoints de scraping (optionnel)
SCRAPE_SECRET_KEY="your-scrape-secret-key"

# ZenRows API (non utilisé en production, uniquement pour fallback)
# ZENROWS_API_KEY="your-zenrows-api-key"
# ZENROWS_KEY="your-zenrows-key"
# URL de votre projet Supabase (ex: https://xxxxx.supabase.co)
# SUPABASE_URL="https://your-project.supabase.co"

# Service Role Key (clé secrète, à garder privée)
# Récupérable dans Settings > API > service_role key
# SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"

# ============================================
# AUTRES (OPTIONNEL)
# ============================================
# Clé secrète pour les endpoints de scraping (optionnel)
SCRAPE_SECRET_KEY="your-scrape-secret-key"

# ZenRows API (non utilisé en production, uniquement pour fallback)
# ZENROWS_API_KEY="your-zenrows-api-key"
# ZENROWS_KEY="your-zenrows-key"

